/**
 * Theme barrel — optional: `import { colors, fonts, layout, reading } from '../theme'`
 * Single source of truth: ./appTheme.js
 */
export {
  colors,
  fonts,
  layout,
  reading,
  tagColorsForInterestId,
  tagColorsForTagString,
} from './appTheme';
export { default } from './appTheme';
